/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Allow importing the legacy CSS from root-level /css/main.css
  transpilePackages: [],
};

export default nextConfig;
